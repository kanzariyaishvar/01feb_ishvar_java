#include<stdio.h>
void main()
{
    int a, b;
    printf("Enter the value of a and :");
    scanf("%d%d",&a,&b);
    if(a,b)
    {
        printf("both are equal");
    }
    else
    {
        printf("both are not equal");
    }
}
